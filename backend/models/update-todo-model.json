{
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name des To-Do-Elements"
      },
      "dueDate": {
        "type": "string",
        "format": "string",
        "description": "Fälligkeitsdatum des To-Do-Elements"
      }
    },
    "required": ["name", "dueDate"],
    "additionalProperties": false
  }